# raw/

Immutable source documents. Drop files here for the LLM to ingest via `/update-wiki ingest <path>`.

The LLM reads these files but never writes to them. They are the evidence layer — wiki pages are interpretation.

Current sources used for this wiki were synthesized directly from the live codebase and `docs/` files. Copy stable versions here to create a fixed reference point.
